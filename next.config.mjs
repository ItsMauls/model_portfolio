/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains : ['www.firstforwomen.com', 'milkbarstore.com', 'res.cloudinary.com']
    }
};

export default nextConfig;
